@model TSTuring2015.MVCWeb.Models.SelectDetails

@Ajax.ActionLink(@Model.Title, "Select", new { Selection = @Model.Select }, new AjaxOptions
               {
                   UpdateTargetId = "console",
                   InsertionMode = InsertionMode.Replace,
                   HttpMethod = "GET"
               }
                    )