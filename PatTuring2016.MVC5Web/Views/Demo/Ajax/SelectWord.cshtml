@model PatTuring2016.MVC5Web.Models.SelectDetails

@Ajax.ActionLink(@Model.Title, "SelectWord", new { Choice = 0 }, new AjaxOptions
        {
            UpdateTargetId = "console",
            InsertionMode = InsertionMode.Replace,
            HttpMethod = "GET"
        }
            )
