@using System.Linq
@model System.Collections.Generic.List<PatTuring2016.Common.ScreenModels.PartDisplays.SenseListModel>

@foreach (var item in Model.Where(item => item != null).Where(item => item.Title != "Licensor"))
{
    @Html.Partial("SimplePartials/SimpleMoreSenseList", item)
}